---
title: "Getting Started with Bayesian Inference in Julia"
format: html
jupyter: julia-1.11
---



```{julia}
cd(@__DIR__)
using Pkg
Pkg.activate(".")
Pkg.instantiate()

```

There is a package called `Turing.jl` that is a powerful tool for Bayesian inference in Julia. It allows you to define probabilistic models and perform inference using various algorithms. Compared to the R/Stan ecosystem, Julia + Turing has both upsides and downside. The upside is that everything is written in one language, Julia, so you avoid the headache to come to grips with Stan. One of the downsides is that there are no off-the-shelf techniques for model comparison, so we will have to either implement them ourselves or to use the RCall interface to R.

```{julia}
using Turing
using StatsPlots
using Distributions
using DataFrames
using CSV
using LinearAlgebra
using Pigeons
```


